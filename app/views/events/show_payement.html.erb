<div class="container event-payement">
	<div class="jumbotron">
    <h1><%= @event.name %></h1>
		<%= form_tag payement_create_path do %>
      <article>
        <% if flash[:error].present? %>
          <div id="error_explanation">
            <p><%= flash[:error] %></p>
          </div>
        <% end %>
        <label class="amount">
          <span>Payer pour participer : <%= @event.price %> $</span>
        </label>
      </article>
      <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
        data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
        data-description="Payer pour la soiree"
        data-amount="<%= @event.price %>"
        data-email="<%= current_user.email %>"
        data-locale="auto"></script>
    <% end %>
  </div>
</div>